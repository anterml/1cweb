import { calcTotal, upper } from '$lib/snippets/base'

/* export function oninput(elem: HTMLInputElement, elems: Elems) {
  //elems.Описание.value = elem.value.toUpperCase()
  console.log('hello')
  elems.Описание.value = upper(elem.value)
}


export function onchange(elem: HTMLInputElement, elems: Elems) {
  console.log('onchange name!')
}
*/
/* export const onforminput: OnFormInput = {
  observeFields: ['Количество', 'Цена'],
  handle(currentInput, inputs, triggerInput) {
    if(triggerInput === inputs.Количество) {

    }
    const a = Number(inputs.Количество.value) || 0
    const b = Number(inputs.Цена.value) || 0
    //currentInput.value = String(a * b)
    //return a * b
  }
} */